# stv-core — ミッション (責務定義)

stv-core は speech-to-visuals ファミリーの**コア契約層**。次の 3 つだけを担う:

1. **types/** — 型契約の単一ソース (Diagram / Scene / メトリクス等)
2. **utils/ , lib/** — ドメイン知識ゼロの共通純計算ユーティリティ
3. **config/** — 設定スキーマとバリデーション (env 読み込みの実行はプロダクト側)

履歴は speech-to-visuals から 2026-08-18 に git filter-repo で分割継承した。

## 禁止 (CI が自動検査: `npm run boundary`)

- **製品層への依存 (逆方向)** — 音声認識・分析・レイアウト・動画生成・
  パイプライン・API・UI・監視のいずれも import しない
- **親リポジトリ (speech-to-visuals) への逆依存** — 絶対禁止
- ドメイン変換ロジック・IO オーケストレーション・実行環境知識の持ち込み
- 上記を持ち込みたくなったら、それはこの repo ではなく製品側のモジュール

## 規模ラチェット (CI fail)

| 指標 | 上限 | 根拠 |
|---|---|---|
| 実装行数 (src/, テスト除く) | 6,000 行 | 分割時実績 約 5,100 行 + 余裕 |
| 実装ファイル数 | 90 | 分割時実績 71 ファイル |

上限に達したら**分割を検討する**。上限を引き伸ばさない。
